name=d4

[../src/Main.d]
target=../bin/d4

buildflags=-I../src -S../lib
debugflags+=-g

version( Posix ) {
   # Needed by assimp.
   buildflags+=-llstdc++
}

version( GNU ) {
   buildflags+=-vdmd
   debugflags+=-debug -g -w
   releaseflags+=-release -O -funroll-loops -finline-functions
}
