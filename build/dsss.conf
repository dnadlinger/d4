name=d4

[../src/Main.d]
target=../bin/d4

buildflags=-I../src
debugflags+=-g

version( Posix ) {
   # Needed by assimp.
   buildflags+=-llstdc++
}

version( GNU ) {
   debugflags+=-debug -w
   releaseflags+=-release -O -funroll-loops -finline-functions
}
